# GEO 报告数据更新流程

用户要求更新优化词四页数据时，按以下流程执行：

1. **先向用户要三样信息**（不要用任何默认值或猜测）：
   - GEO 系统的账号、密码
   - 项目名称（或项目 ID）
   - 日期区间（可选；不给则用项目实际有数据的日期范围）

2. 登录后先列出账号下的项目，用项目名称精确匹配到项目 ID，对不上时把项目列表给用户确认，**绝不**擅自选一个相近的。

3. 运行采集脚本（凭据只通过环境变量传入，不写进任何文件）：

   ```bash
   GEO_API_BASE=https://api.geotopone.com GEO_USER=<账号> GEO_PASS=<密码> \
     node scripts/fetch-geo-report.mjs <project_id> [--start YYYY-MM-DD] [--end YYYY-MM-DD]
   ```

4. 四个页面自动读取 `src/data/geoReport.json`，无需改代码：
   优化词总览（OptDashboardHtml）、词条（OptEntriesHtml）、竞品（OptCompetitorHtml）、引用源（OptSourcesHtml）。

5. **必须**同步改写竞品页 `Page_SkyworthReport_OptCompetitorHtml.jsx` 底部三张手写分析卡
   （核心发现 / 竞争格局总结 / 行动建议），依据最新排名数值撰写，数字要与榜单一致。

6. 用 `node scripts/shot-page.mjs <slide-id> <输出路径>` 截图逐页核对后，按每页一次提交。

## 总览四页（核心数据总览 / 监测词总览 / 报告说明 / 监测范围）

这四页读取 `src/data/geoOverview.json`，由汇总脚本一次采集账号下全部 12 个项目
（品类优化词 / 品类监测词 / 5 个产品优化词 / 5 个产品监测词，按项目名与 group_name 自动识别）：

```bash
GEO_API_BASE=https://api.geotopone.com GEO_USER=<账号> GEO_PASS=<密码> \
  node scripts/fetch-geo-overview.mjs
```

数据来源口径：
- 提及率：`/api/conversations/stats` 的 `brand_mention_rate`
- TOP1 / TOP3 提及率：`/api/competitors/top-mention-rate?top_type=top1|top3` 取 `is_self` 条目
- 正面 / 负面比例：`/api/sentiments/stats` 的 `positive_percentage` / `negative_percentage`
- 监测范围：词条数来自项目列表 `entry_count`，查询次数与文章数来自各项目 `/api/citations` 汇总

更新数据后**必须**人工同步改写：
- 核心数据总览页（`Page_SkyworthReport_CoreDataSelf.jsx`）底部的「数据总结」段落
- 监测词总览页（`Page_SkyworthReport_MonitorWordOverview.jsx`）的 `PRODUCT_NOTES` 各产品短评

竞品对比页（`Page_SkyworthReport_CoreDataCompetitor.jsx`）目前仍是手写数据，不在此流程内。
