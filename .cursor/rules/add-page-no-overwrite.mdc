---
description: 新增页面时不得覆盖或删除原页面
alwaysApply: true
---

# 新增页面规则

## 新增页面：插入，不覆盖

- 需要新增一页时，把新页面插入到**指定序号之前**，原来在该序号及之后的页面依次往后顺延。
- **绝不**用新页面覆盖、替换该序号原有的页面。

## 删除页面：没明确指令就不许删

- 在用户**没有明确指示删除**的情况下，**绝不**删除任何原有页面。
- 不确定是插入还是替换时，先问用户，不要擅自删。
