---
description: 每改完一页后自动本地提交 git，不推送远程
alwaysApply: true
---

# 每页改完后自动本地提交

改完一页（一个 slide / page 文件及其相关改动）后，自动执行本地 git 提交：

- 用 `git add` 添加本次这一页的相关改动，`git commit` 提交。
- commit message 用中文，简要说明改了哪一页、改了什么。
- 每完成一页就提交一次，不要把多页的改动攒到一个提交里。
- 只做本地提交，**绝不** `git push` 到远程。除非用户明确要求推送。
